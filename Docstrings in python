# Task 1 

user_input = input("What is your name? ")
name = user_input

def greet_user(name): 
    '''
    The function would greet the user 
    
    (name) this is str (string) which will than 
    take the users input and greet them
    
    the return would be none 
    '''
    print("Hello, " + name + "!")
    
greet_user(name)
    
# Task 2 

def square(number):
    """
    Calculate the square of a number 
    (number) = either a int or float = the number to be squared 
    Return either a int or a float whatever the number that was inputted to be squared
    """
    return number ** 2 








